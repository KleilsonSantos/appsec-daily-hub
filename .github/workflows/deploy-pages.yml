name: 🚀 Deploy para GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonando repositório (checkout)
        uses: actions/checkout@v3

      - name: 📦 Publicando para GitHub Pages (branch gh-pages)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN || env.GITHUB_TOKEN }}
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ env.GH_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          force_orphan: true
          commit_message: "🚀 Deploy automático para GitHub Pages"