name: Update CVE List

on:
  schedule:
    - cron: '0 */12 * * *' # A cada 12 horas
  workflow_dispatch:

jobs:
  update-cves:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout o repositório
        uses: actions/checkout@v3

      - name: Instalar dependências
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Buscar últimos CVEs e atualizar README.md
        run: |
          echo "🔍 Buscando últimos CVEs..."
          curl -s "https://services.nvd.nist.gov/rest/json/cves/2.0?cvssV3Severity=CRITICAL&resultsPerPage=5" -o cves.json

          echo "## 📊 Últimos CVEs Monitorados (Atualizado automaticamente)" > cve_block.txt
          echo '```yaml' >> cve_block.txt
          jq -r '.vulnerabilities[] | "- \(.cve.id) - \(.cve.descriptions[0].value)"' cves.json | sed 's/^/  /' >> cve_block.txt
          echo '```' >> cve_block.txt
          echo "📝 Atualizando README.md com os últimos CVEs..."


          awk '
            BEGIN {inside=0}
            /## 📊 Últimos CVEs Monitorados/ {print; while ((getline line < "cve_block.txt") > 0) print line; inside=1; next}
            inside && /^```/ {inside=0; next}
            !inside {print}
          ' README.md > temp.md && mv temp.md README.md

          echo "✅ Bloco atualizado com sucesso!"

      # - name: Commit e push se houver mudanças
      #   env:
      #     GH_TOKEN: ${{ env.GITHUB_TOKEN }}
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "github-actions[bot]@users.noreply.github.com"


      #     REPO="github.com/KleilsonSantos/appsec-daily-hub"
      #     git remote set-url origin https://x-access-token:$GH_TOKEN@$REPO

      #     git add README.md
      #     git diff --cached --quiet || git commit -m "🔄 Update CVE list [bot]"
      #     git push origin HEAD:main || echo "Nenhuma mudança para enviar."

      # - name: Notificar conclusão
      #   run: echo "✅ Atualização de CVEs concluída com sucesso!"
      #   env:
      #     GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # - name: Commit e push se houver mudanças
      #   env:
      #     GH_TOKEN: ${{ secrets.GITHUB_TOKEN || env.GITHUB_TOKEN }}
      #     REPO_NAME: ${{ github.repository }}
      #   run: |
      #     set -e
      #     echo "🔑 Verificando token..."

      #     if [ -z "$GH_TOKEN" ]; then
      #       echo "❌ GH_TOKEN não está definido. Abortando push."
      #       exit 1
      #     fi

      #     git config user.name "github-actions[bot]"
      #     git config user.email "github-actions[bot]@users.noreply.github.com"

      #     REPO_URL="https://x-access-token:$GH_TOKEN@github.com/$REPO_NAME"
      #     git remote set-url origin "$REPO_URL"

      #     git add README.md
      #     git diff --cached --quiet || git commit -m "🔄 Update CVE list [bot]"
      #     git push origin HEAD:main || echo "⚠️ Nenhuma mudança para enviar."

      - name: Commit e push se houver mudanças (Local ACT)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_NAME: ${{ github.repository }}
        run: |
          echo "🔑 Verificando token..."
          if [ -z "$GH_TOKEN" ]; then
            echo "❌ GH_TOKEN não está definido. Abortando push."
            exit 1
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:$GH_TOKEN@github.com/KleilsonSantos/appsec-daily-hub
          git add README.md
          git diff --cached --quiet || git commit -m "🔄 Update CVE list [bot]"
          git push origin HEAD:main || echo "⚠️ Nenhuma mudança para enviar."
      - name: Notificar conclusão
        run: echo "✅ Atualização de CVEs concluída com sucesso!"